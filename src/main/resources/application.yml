spring:
  application:
    name: online-store
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/online_store
  security:
    jwt:
      secret: VeryStrangeSecretForJWTHS512SignatureAlgorithm64ByteLength
      access-expiration: 1
      refresh-expiration: 24